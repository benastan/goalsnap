%h1 Listing tallies

%table
  %tr
    %th Type
    %th Title
    %th Redeemed?
    %th
    %th
    %th

  - @tallies.each do |tally|
    %tr
      %td= tally.axis_type
      %td= tally.axis.title
      %td= tally.user_id
      %td= tally.has_been_spent ? :yes : :no
      - if current_user.admin?
        %td= link_to 'Edit', edit_tally_path(tally)
        %td= link_to 'Destroy', tally, method: :delete, data: { confirm: 'Are you sure?' }

%br
